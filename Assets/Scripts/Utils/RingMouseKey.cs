using UnityEngine;

namespace TOM.Common.Utils
{
    public static class RingMouseKey
    {
        public const KeyCode UP_ACTION_KEYCODE = KeyCode.PageUp;
        public const KeyCode DOWN_ACTION_KEYCODE = KeyCode.PageDown;
        public const KeyCode LEFT_ACTION_KEYCODE = KeyCode.Space;
        public const KeyCode RIGHT_ACTION_KEYCODE = KeyCode.Period;
        public const KeyCode CENTER_ACTION_KEYCODE = KeyCode.F5;
    }
}
